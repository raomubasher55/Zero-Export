# Same-origin defaults use Vite's development proxy for /api/v1 and /health.
VITE_API_BASE_URL=/api/v1

# For a separately deployed API, use absolute URLs without trailing slashes.
# VITE_API_BASE_URL=http://localhost:3001/api/v1
# VITE_HEALTH_URL=http://localhost:3001/health
